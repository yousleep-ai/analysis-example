# Pinned exactly, and a real analysis should do the same.
#
# The platform runs an image by content digest, so what matters is that this
# image is reproducible rather than current: a range that resolves differently
# next month gives you an image you cannot rebuild. Bump these deliberately,
# rebuild, and re-pin the digest in the analysis configuration.
mne==1.13.2
yousleep-common==28.3.0
